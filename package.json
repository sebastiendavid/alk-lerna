{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna:publish": "lerna publish from-package minor --yes --registry http://localhost:4873/",
    "lerna:test": "lerna run test --since master"
  },
  "devDependencies": {
    "lerna": "~3.6.0"
  }
}
