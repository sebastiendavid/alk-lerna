{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna:publish": "lerna publish patch from-package --yes --registry http://localhost:4873/",
    "lerna:test": "lerna run test --since master"
  },
  "devDependencies": {
    "lerna": "^3.5.1"
  }
}
